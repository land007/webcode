# 运行模式: desktop（完整桌面）/ lite（仅 Theia + Vibe Kanban，无 VNC 桌面）
MODE=desktop
# VNC 密码（留空自动生成，仅 desktop 模式有效）
VNC_PASSWORD=
# VNC 分辨率（默认 1920x1080，仅 desktop 模式有效）
VNC_RESOLUTION=1920x1080
# Basic Auth 用户名（用于 Theia / Vibe Kanban / OpenClaw 访问认证）
AUTH_USER=admin
# Basic Auth 密码（默认 changeme）
AUTH_PASSWORD=changeme
# Git 提交用户名和邮箱（留空则不设置）
GIT_USER_NAME=
GIT_USER_EMAIL=
# Cloudflare Tunnel token（设置后自动启用，留空则不启动）
# CF_TUNNEL_TOKEN=eyJ...
